# PantryPilot — required credentials (fill in your own; never commit real values)

# Telegram — create a bot via @BotFather to get these
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
TELEGRAM_CHAT_ID=YOUR_TELEGRAM_CHAT_ID

# Groq — for Whisper transcription + LLM understanding/selection
GROQ_API_KEY=YOUR_GROQ_API_KEY

# Swiggy MCP — obtained via the Swiggy MCP auth flow (mcp-remote bridge)
SWIGGY_MCP_TOKEN=YOUR_SWIGGY_MCP_TOKEN
SWIGGY_ADDRESS_ID=YOUR_SWIGGY_ADDRESS_ID
